import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'hoteles_de_pacifico',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
